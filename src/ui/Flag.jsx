import styled from "styled-components";

export const Flag = styled.img`
  max-width: 2rem;
  border-radius: var(--border-radius-slim);
  display: block;
  border: 1px solid var(--color-grey-100);
`;
